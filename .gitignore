.venv/
__pycache__/
*.pyc
.pytest_cache/
runs/
logs/
cache/
settings.json
STOP_TRAINING.txt
*.log

# calibration is machine/install-specific measurement output
# (committed deliberately for the reference car -- see calibration/etk800.json)

# GUI field memory -- machine-local, like settings.json
gui_state.json

# experiment outputs: rollout datasets, result tables, probe captures
*.npz
*.csv
!calibration/*.csv
!tests/fixtures/*.csv
!docs/data/**/*.csv
evaluations/
experiments/
best_models/
.gui-configs/

# parked copies live outside the repo, never commit one
*.BAK*
*_BAK
